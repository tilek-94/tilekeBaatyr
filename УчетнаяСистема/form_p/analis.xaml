<Page x:Class="УчетнаяСистема.form_p.analis"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:УчетнаяСистема.form_p"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="analis" Loaded="Page_Loaded">

    <Grid Background="White">
        <Border Grid.Column="1" Padding="5" Background="#F4F4F4">
            <StackPanel  Background="White" >
                <DockPanel HorizontalAlignment="Stretch" Background="#F4F4F4">
                    <Label Content="KG Group Компаниясы" FontSize="16" Foreground="#00C3FF" ></Label>
                    <TextBox FontSize="14"  Style="{DynamicResource placeHolder}" Tag=" Поиск данные" Height="25" Width="200" Margin="5" HorizontalAlignment="Right">

                    </TextBox>
                </DockPanel>
                <DataGrid FontSize="12"  Style="{DynamicResource grid_xml}" AutoGenerateColumns="False"
                  HeadersVisibility="Column"
                  ColumnHeaderStyle="{DynamicResource hederstyle}" 
                         x:Name="dataGridView1"
                          CanUserAddRows="False"
                          ItemsSource="{Binding}"
                          >

                    <DataGrid.Columns>
                        <DataGridTextColumn Header="№" Binding="{Binding id}" />
                        <DataGridTextColumn Header="Марка" Binding="{Binding number_f}" />
                        <DataGridTextColumn Header="Номер машины" Binding="{Binding floor}" />
                        <DataGridTextColumn Header="Дата" Binding="{Binding kvm}" />
                        <DataGridTextColumn Header="Цена на долларах" Binding="{Binding contract}" />
                        <DataGridTextColumn Header="Курс валюта" Binding="{Binding name}" />
                        <DataGridTextColumn Header="Курс валюта" Binding="{Binding marka}" />
                        <DataGridTextColumn Header="Курс валюта" Binding="{Binding kurs}" />
                        <DataGridTextColumn Header="Курс валюта" Binding="{Binding price_kvm}" />
                        <DataGridTextColumn Header="Курс валюта" Binding="{Binding som}" />
                        <DataGridTextColumn Header="Курс валюта" Binding="{Binding us}" />
                        <DataGridTextColumn Header="Курс валюта" Binding="{Binding kg}" />
                    </DataGrid.Columns>


                    


                </DataGrid>
            </StackPanel>
        </Border>


    </Grid>
</Page>
